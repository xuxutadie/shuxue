# 在线教学版验证记录

## 后续修复：无学生时备课（2026-09-11）

移除课程与教案的学生数量限制；教师在没有学生、甚至尚无班级时也能查看12节课程及全部课堂环节。课程卡片新增教案直达入口；无学生时不显示完成标记、评价保存和课堂记录保存操作。添加学生后原记录功能保持可用。

新增3组回归测试覆盖课程目录、12节课×5个环节和已有学生记录功能；连同账号及数据库测试共11组全部通过。浏览器使用实际教师账号确认12个教案入口，打开第一课的教案、练习答案及游戏，没有新增测试学生。

日期：2026-09-11。验证环境：Windows、Node.js 24.11.1、真实PostgreSQL 17.6。

## 已完成

- 后端账号、班级、师生权限、数据库会话、首次改密、重置密码与退出。
- 服务器自动保存答案、修订号冲突检测、45分钟截止、幂等交卷、120分计分、教师控制讲评。
- 12节课程、36道练习、7个游戏、教师讲课评价、学生档案及知识点表现。
- 独立在线入口8766，原本地查阅入口8765改为静态文件白名单。
- 卡通几何角色、多巴胺配色、六周探索地图、明确的师生身份和探索徽章。
- 旧档案预览及对应导入、原版本历史成绩、演示档案排除、数据库备份和恢复材料。
- 生产容器、数据库普通应用账号、HTTPS反向代理示例、使用说明。

## 自动测试

`node scripts/local-runtime.cjs test`：7组集成测试通过，0失败。涵盖未登录/越权拒绝、强制改密、CSRF与来源校验、首次练习结果和重做、教师评价保护、测评分配、答案隐藏、全对120分、过期0分、保存冲突、重复交卷、答案开放、旧版导入、防重复导入、重置会话失效及私有文件不可下载。

原有 `tests/core.test.cjs` 与 `tests/flow.test.cjs` 均通过，既有40题、36道练习、课时及历史成绩规则不变。

所有新增服务端与前端脚本通过语法检查。经官方npm审计接口检查，安装依赖未发现已知漏洞（本次查询结果，不是永久安全保证）。

## 浏览器实测

使用专门的 `math_lab_ui_test` 数据库与8767端口；测试账号不在正式库。

- 学生登录仅显示个人空间，没有教师身份切换或其他学生选择。
- 讲课准备保存后刷新仍存在；教师端能够读取。
- 第1课第1题先答99再答2，显示首次未答对、共两次作答。
- A卷20个答题输入框；填写前两题、保存并刷新后提交，服务器计12分；未开放时学生看不到解析。
- 教师端可查看12分记录、逐题答案和解析，开放后学生可查看同份讲评。
- 教师选择“独立讲清”并保存，成长档案显示1次独立讲清；标记完成后显示1/12课。
- 循环游戏反馈与探索记录保存。
- 学生首页在320、768、1024、1440宽度均无页面横向溢出；教师档案和班级页320宽度复查通过，表格可在自身区域横向滚动。
- 最后一轮浏览器错误日志为空。

发现并修复：旧版讲课评价选项HTML结构缺失；新版教师可正常选择与保存。窄屏档案卡片原有7像素溢出已修复。

## 备份恢复

分别备份正式初始库和界面测试库，恢复到新建的独立校验数据库。含数据的校验结果为：3个账号、2个学生、1份测评，与恢复前一致。未覆盖正式库。

新旧网站请求 `.runtime/local-config.json` 均返回404；新版题库源码、服务器文件、配置与初始化密码文件也返回404。

## 尚未实施的外部上线步骤

- 用户尚未提供公网服务器、域名和部署地区；当前仅本机运行，没有公网地址。
- 本机Docker工具启动失败，改用官方PostgreSQL原生程序完成数据库实测。容器构建、HTTPS证书及目标服务器配置尚未在生产环境运行验证。
- 没有批量并发负载测试；首版按单应用实例设计。
- 正式视频尚未制作；当前为图文课件与视频播放器/地址设置。
- 旧浏览器记录尚未导入正式账号，需要教师在界面核对学生对应关系。
- 定时备份、证书续期和服务监控需在确定服务器后配置。

首次教师账号已创建，初始凭据仅保存在本机 `.runtime/教师首次登录-teacher.txt`，文档和工具输出未打印密码。未修改.env，未提交Git。


## 详细教案、讲堂支架与SVG课堂（2026-09-11）

本轮扩充课堂视图，新增SVG实验模块与12课教案数据，保持原数据结构与权限边界。

自动检查：在线集成测试8项和课堂测试7项，共15项通过。覆盖12课全部5种备课页面、无学生访问、有学生评价入口、120分钟安排与动画先于学生讲课、原36题和40题完全一致，以及因数余数、周期整除、同周期闪灯、平方单位、等速无法追上、草地不耗尽等边界。遍历全部模型的参数范围和演示进度，没有无效数值或负尺寸SVG。

浏览器实测：

- 第一课知识讲解先显示导入与SVG，再显示4步推导；调整笔价由2到3元时，订单由13/17元变为16/22元，价差与公式同步改变。
- 教师在互动验证输入4并检查，正确显示验证成功；继续按钮进入四步提纲、开场白与追问。
- 第一课教案有完整9个部分，课表从0到120分钟，测评45分钟，动画和验证先于上台讲解。
- 追赶动画真实播放后，两人位置随时间变化；在第10分钟暂停，两人图形坐标一致，按钮恢复为播放状态。
- 首课讲解和教案在实际浏览器宽度没有页面横向溢出；未对本轮所有页面重新进行全尺寸设备矩阵测试。

没有通过本轮操作写入正式学生成绩或教师评价。教师课件支持互动动画；正式录制视频不属于本轮交付。


## 母题先于讲解（2026-09-11）

补入12组完整母题、24道完整变式。在线与课堂检查共18项通过；覆盖12课的5种入口，确认母题完整题干与全部所求均存在，并位于分析、讲堂提示或独立练习输入之前。知识讲解、教师教案和练习页均有两道完整变式；学生练习不展开变式解析，原3个作答入口保持有效。原36题与40题内容的逐项对比仍通过。
