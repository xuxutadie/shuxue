# 课程视频随网站部署

默认课程视频不依赖本机绝对路径或第三方视频页面。正式可播放文件位于 `public/media/lessons/`，使用网站同源的 `/media/lessons/…` 地址。现有 Dockerfile 会随 `public` 一起复制媒体，部署网站时必须保留该目录。

## 上线时保留这些文件

- `public/media/lessons/`：当前清单引用的12条MP4、12份VTT字幕和12张封面；目录中还保留此前版本，便于回退。
- `public/course-videos.js`：默认资源与章节清单。
- `public/views.js`、`public/app.js`、`public/styles.css`、`public/index.html`：播放器及章节交互。
- `server/content.js`、`server/curriculum-revision.json`、`server/video-guides.json`：新版课程与教师专用末题解法，均由服务端按角色提供。
- `server/practice-history.js`、`server/learning.js`：新版练习与旧答题记录的区分和保存逻辑，需与前端一起更新。

视频以H264/AAC封装为MP4，1920×1080、24帧/秒，已把播放索引移到文件开头。Express静态服务支持HTTP Range，可拖动进度与跳章节。反向代理应保留Range请求和206响应，字幕返回 `text/vtt`。不需要新增数据库表或重新给每个班级填写视频地址。

## 日后替换与扩展

单个班级替换：在「班级与资源 → 课程设置」填写MP4/WebM直链。清空后恢复内置视频。视频地址需允许浏览器直接读取，HTTPS网站应使用HTTPS地址。

系统视频替换：使用新的版本文件名，更新默认清单中的视频、字幕、封面与章节；不要仅覆盖旧文件而保留原缓存地址。字幕与章节必须跟随新片时间更新。

本次第2、4、5、6、8、9、10、11、12课采用v2地址；第1、3、7课继续使用已确认的v1成片。运行发布入口前会核对每条视频与当前制作源文件是否一致，未完成的成片不会进入清单。

制作源文件位于 `videos/course-series/`，包含逐课旁白、图示、时间轴、检验记录和可重做的制作入口；第一课沿用 `videos/lesson-01-pairs/` 的已验收成片。制作目录不作为网站静态目录，也不需要部署，已从Docker构建上下文排除。

如果以后迁到对象存储，可将默认清单地址替换为同一版本的媒体直链，章节和播放器逻辑可以继续使用。当前本机接入完成不代表已经部署到公网，公网访问仍需使用项目既定的部署流程。
