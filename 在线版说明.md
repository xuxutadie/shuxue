# 思维实验室 · 在线账号版

## 先打开新版

在本机运行 `start-online.ps1`，访问 **http://127.0.0.1:8766**。

已有本机教师使用原账号登录。新部署不会附带本机账号、密码或学生数据，需要按下文步骤初始化教师账号。

这个地址是本机预览，学生在其他电脑还不能通过它访问。公网服务器、域名尚未配置，不等于已经发布上线。

原来的 http://127.0.0.1:8765 保留为本地查阅、导出入口，运行 `start.ps1` 启动。两版入口及数据来源不同。新版不会自动把原浏览器里的档案当成正式账号。

## 第一次使用：教师做这五步

1. 登录教师账号并修改初始密码。
2. 在“班级工作台”点击“创建学生账号”。输入姓名、登录账号、初始密码和班级。账号使用字母、数字、下划线或短横线，3至40位；密码10至128个字符。
3. 把每位学生自己的账号和初始密码交给本人。学生第一次登录也必须改密。
4. 点击“布置班级测评”，为班级发布前测A卷。学生点击开始时才计时；老师布置时不会提前计时。
5. 学生交卷后，点击学生姓名或“学生测评”，查看成绩、逐题答案和解析。需要讲评时点击“向学生开放讲评”。

学生之间的数据独立；教师只能管理自己创建的班级和学生。页面顶部始终显示账号身份，没有学生切换到教师的按钮。

## 一节课怎么用

教师在“课程与教案”中备课，未创建班级或学生时也能查看全部12节课程、教师教案、练习答案，体验游戏及打印资料。课程卡片上的“查看教师教案”可直接进入教案。

选择学生后才出现课堂完成标记、学生讲课评价和课堂记录保存。授课时按四个环节推进：知识讲解、学生当小老师、游戏验证、独立练习。

- 学生在“小老师讲堂”保存讲课关键词。教师看到学生准备后，选择“独立讲清”“追问后讲清”或“需要重新学习”，点击“保存讲课评价”。
- 教师可在“教师教案”填写课堂记录、打印教案与学生练习卷，并标记课堂完成。
- 学生练习由服务器检查答案，保留首次结果、累计尝试次数与最近100次作答明细。做对后展示讲解；做错的题进入练习与错题。
- 游戏是探索活动，记录参与情况，不计入测评分数。徽章也不影响成绩。

当前包含12节图文课程、36道练习、40道测评题、7个活动。正式配音视频尚未制作；教师可在“班级与资源 → 课程设置”接入HTTPS的MP4/WebM直链。学生将看到自己班级的日期和视频。

## 测评规则

- 前后测各20题，每题6分，满分120分，连续45分钟。
- 允许纸笔草稿，不使用计算器、AI或教师提示。
- 答案自动发送到服务器，顶部显示保存状态。看到“已保存到服务器”才表示保存成功。
- 断网时会提醒，并在重连后重试；关闭页面前若有未保存答案，浏览器会提醒。没有实现离线考试，断网期间关闭浏览器会丢失尚未发送的输入。
- 计时以服务器截止时间为准。离开页面也继续计时；到期后台按已保存答案自动交卷，最多有约5秒的结果展示延迟，截止后的修改仍会被拒绝。
- 两个页面同时改同一份试卷时，会阻止旧页面覆盖新记录，并要求载入最新记录。请先记下自己未保存的输入再处理冲突。
- 交卷后不能修改或再次开始同一份在线测评。重复点击提交不会生成两份成绩。当前产品以一次前测、一次后测为流程，另行重测需后续增加独立测评批次。
- 学生可见分数；逐题解析由教师控制开放。未开放时，答案不会包含在学生接口响应中。

## 教师看什么

班级工作台显示学生数、前后测完成情况和待跟进任务。可按班级筛选、按姓名或账号搜索。

学生成长档案包括：课程进度、前后测得分及变化、知识点答对题数、练习首次表现、重做情况、学生讲课准备、教师评价及课堂记录。知识点表现依据本卷题目，用于调整教学，不直接预测比赛成绩。

教师可以重置自己班级学生的密码；重置后所有旧登录失效，学生再次登录需修改初始密码。

## 导入原来的学习记录

1. 在原8765本地版进入教师中心，导出JSON备份。请使用原先保存学习记录的那个浏览器。
2. 在新版创建对应学生账号。
3. 教师进入“班级与资源 → 导入旧版档案”，选择备份文件和来源学生。
4. 点击“预览所选档案”，核对来源姓名、课程数、练习数、试卷版本和分数。
5. 选择对应的在线学生，确认导入。

已有在线学习记录优先保留，旧测评进入“迁入的历史测评”，按原版本重新核对分数，不替代在线前后测。重复导入同一档案会被拦截。旧版没有记录的首次练习表现不会凭空补造。旧版正在作答的草稿不迁移，请先在原版完成。

“验收”“演示”档案不导入正式统计。此次浏览器测试另用独立数据库，正式班级没有演示学生。

## 数据备份

教师网页中的“导出学习记录”用于教学查阅和资料保存，不包含密码和登录会话。该导出不是完整的数据库恢复文件。

本机数据库完整备份：

```powershell
node scripts/local-runtime.cjs run scripts/backup.cjs
```

文件写入 `backups` 目录，包含账号与学习数据，应妥善保管。将备份复制到另一台受控设备，避免只有同一块硬盘上的一份数据。不要把整个项目目录公开为静态网站。

本轮已把备份恢复到单独的新数据库，并核对账号、学生、测评数量；没有覆盖正在使用的库。

## 公网部署

已提供 `Dockerfile`、`compose.yaml` 和 `deploy/nginx.conf.example`。生产配置要求HTTPS，数据库不对公网开放，应用通过独立数据库账号连接。启动时自动创建本版表结构。

需要准备：可运行Docker的服务器、域名、有效HTTPS证书、两个不同的数据库密码，以及公网网站地址。具体部署位置取决于师生所在地区。本轮没有购买服务器、注册域名或把学生信息上传到外部服务。

在服务器的安全环境变量或密钥管理中提供以下值，不要写入聊天或提交仓库：

| 变量 | 用途 |
|---|---|
| `MATH_DB_ADMIN_PASSWORD` | 数据库管理员密码 |
| `MATH_DB_PASSWORD` | 应用专属数据库密码；建议生成至少32位十六进制随机串，避免连接URL转义问题 |
| `MATH_APP_ORIGIN` | 例如 `https://math.example.com`，不要加末尾斜杠 |

然后在服务器运行：

```sh
docker compose up -d --build
```

首次初始化教师：先在服务器安全设置 `INITIAL_TEACHER_PASSWORD`，再运行：

```sh
docker compose exec -e INITIAL_TEACHER_PASSWORD app node server/create-teacher.js teacher 老师
```

网页首次登录后修改初始密码。账号已存在时初始化命令会拒绝覆盖。不要使用本机测试账号或本机数据库管理密码上线。

根据示例配置Nginx域名和证书，转发到本机8766端口，检查网站登录和 `/health`。应用只信任这一层反向代理，不要把8766端口直接暴露到公网。数据库仅在容器内部网络开放。

完整备份可用：

```sh
docker compose exec -T db pg_dump -U postgres -d math_lab -Fc > math-lab-backup.dump
```

恢复应先在新数据库演练：

```sh
docker compose exec -T db createdb -U postgres math_lab_restore
docker compose exec -T db pg_restore -U postgres -d math_lab_restore --exit-on-error < math-lab-backup.dump
```

核对后再安排维护窗口切换数据库。不要直接对现有正式库执行覆盖恢复。上线后需由服务器运维设置定时备份、备份保留周期、证书续期和服务监控；本轮未在未知服务器上创建这些任务。

## 开发与验证

本机使用官方Windows PostgreSQL 17.6程序作开发验证，生产容器使用PostgreSQL 17系列镜像；部署时应拉取并记录经过验证的当前维护版本。本机数据库文件在 `.runtime`，为处理Windows中文路径，另建英文路径联接指向同一目录，没有复制学生记录到其他服务。

```powershell
node scripts/local-runtime.cjs test
node tests/core.test.cjs
node tests/flow.test.cjs
```

集成测试在真实PostgreSQL上运行随机前缀账号，清理范围仅限该次生成的测试记录。界面演练使用独立的 `math_lab_ui_test` 数据库和8767端口，不属于正式教学。

配置限制：当前是单应用实例部署，未做大规模并发压测；生产容器和HTTPS部署尚未在目标服务器上实测。没有手机短信、邮件找回、AI接口或自动生成视频。这些均未在本轮擅自接入。


## 课堂内容与动画更新（2026-09-11）

12节课统一按“知识讲解与动画观察 → 互动验证与讲课准备 → 小老师上台 → 独立练习”递进。普通课和测评课均保持120分钟；第1、11课先完成45分钟独立测评，再进入教学。

- 知识讲解：问题导入、前置提问、4步例题推导、核心概念、板书路线、易错提醒、变式题与参考思路。
- 教师教案：开场检查、分钟安排、逐步讲述、动画操作建议、学生讲堂示范和追问、具体补救、板书、3道练习解析、课末检查。可直接打印，无学生也能备课。
- 小老师讲堂：开场白、上台前3件准备、四步填空式提纲、完整示范、卡顿提示与评价清单。动画可再次展开；不要求背稿。学生提纲与教师评价仍分别保存。
- 动画：10种原生SVG模型覆盖12课，包括订单消去、复制订单、周期列车、因数分组、闪灯公倍数、补形、半格拼合、追赶、牛吃草、书店枚举。原7个游乐场入口也改用互动SVG。
- 操作：拖动参数、逐步前进或返回、播放/暂停、查看终点、重置；图形和计算同步变化。计算依据默认折叠。参数仅用于当前页面实验，切换页面会恢复该课初始条件；正文例题采用原始条件。
- 互动验证随当前参数生成检查问题。只记录参与，不计入120分测评；老师操作不会记成学生完成。

本次没有新增依赖或调整数据库结构，没有更换原36道练习和40道测评。正式录制视频仍需另行制作或在资源设置中接入。


## 学生密码长度调整（2026-09-11）

学生初始密码、学生自行修改密码、老师重置学生密码均至少6位，允许6～10位，也保留更长密码（最多128字符）。首次登录与老师重置后的首次登录仍需更换初始密码，之后无需每次更改。教师新密码设置规则仍至少10位，既有教师登录密码不变。本次不重置任何真实账号密码。已通过包括六位密码完整流程在内的16项测试。


## 完整母题与变式（2026-09-11）

每课新增1组完整母题与2道完整变式，共12组母题、24道变式。题干写清背景、全部条件、单位与所求，多知识点课使用分问题母题组。教师教案、知识讲解、互动验证、小老师讲堂和独立练习都先呈现完整母题，再进入本环节。

知识讲解在推导之后提供母题逐问答案，并展示两道完整变式；展开后可比较改了什么条件、怎样调整方法与参考答案。教师教案包含相同母题、变式及其完整解析，便于授课和打印。独立练习及打印学生卷先给母题和变式题干，不附带这部分解析；下方原3题继续保存个人练习记录。母题与变式用于口头或纸上教学，不额外计入成绩。

原36道计分课堂练习、40道前后测题及其历史记录均保持原样。每课时长仍为120分钟；两道变式可按掌握情况择用或安排课后，避免在课堂内机械增加题量。
